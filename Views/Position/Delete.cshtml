@model DepartmentPosition

<div class="modal fade" id="deleteModal" role="dialog" aria-labelledby="deletelModalLabel">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header text-center">
                <h3 class="modal-title" id="deletelModalLabel">Xóa chức vụ</h3>
            </div>
            <div class="modal-body text-center">
                <div id="deleteMessage" class="mt-3"></div>
                <form id="deleteForm">
                    <input type="hidden" asp-for="Id" id="deleteId" />
                    <p>Bạn có muốn xóa chức vụ này không?</p>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-danger" id="btnDelete" data-dismiss="modal" onclick="Position.remove()">Xóa</button>
                <button type="button" class="btn btn-secondary" data-dismiss="modal" onclick="Position.resetForm()">Đóng</button>
            </div>
        </div>
    </div>
</div>